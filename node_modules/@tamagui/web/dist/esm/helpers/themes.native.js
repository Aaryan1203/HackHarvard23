import { createVariable, isVariable } from "../createVariable";
function ensureThemeVariable(theme, key) {
  const val = theme[key], themeKey = key;
  isVariable(val) ? val.name !== themeKey && (theme[key] = createVariable({
    key: val.name,
    name: themeKey,
    val: val.val
  })) : theme[key] = createVariable({
    key: themeKey,
    name: themeKey,
    val
  });
}
export {
  ensureThemeVariable
};
//# sourceMappingURL=themes.js.map
