import { isWeb, styled } from "@tamagui/core";
import { SizableText } from "@tamagui/text";
import { Linking } from "react-native";
const AnchorFrame = styled(SizableText, {
  name: "Anchor",
  tag: "a",
  accessibilityRole: "link"
}), Anchor = AnchorFrame.styleable(
  ({ href, target, ...props }, ref) => <AnchorFrame
    {...props}
    {...isWeb ? {
      href,
      target
    } : {
      onPress: (event) => {
        var _a;
        (_a = props.onPress) == null || _a.call(props, event), href !== void 0 && Linking.openURL(href);
      }
    }}
    ref={ref}
  />
);
export {
  Anchor
};
//# sourceMappingURL=Anchor.js.map
