import {
  themeable,
  useTheme,
  variableToString
} from "@tamagui/core";
import { YStack } from "@tamagui/stacks";
import * as React from "react";
import { ActivityIndicator } from "react-native";
const Spinner = YStack.extractable(
  themeable(
    React.forwardRef((props, ref) => {
      const { size, color: colorProp, ...stackProps } = props, theme = useTheme();
      let color = colorProp;
      return color && color[0] === "$" && (color = variableToString(theme[color])), <YStack ref={ref} {...stackProps}><ActivityIndicator size={size} color={color} /></YStack>;
    }),
    {
      componentName: "Spinner"
    }
  )
);
export {
  Spinner
};
//# sourceMappingURL=Spinner.js.map
